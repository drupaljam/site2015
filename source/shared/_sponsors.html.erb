<div class="sponsors">
  <div class="sponsors-inner">
    <span>Sponsoren van DrupalJam 2015</span>
    <% data.sponsors[sponsor_type].each do |sponsor| %>
      <a href="<%= sponsor['url'] %>" class="sponsor-<%= sponsor['key'] %>"><%= sponsor['name'] %></a>  
    <% end %>
  </div>
</div>